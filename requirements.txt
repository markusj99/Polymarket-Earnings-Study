# Third-party deps required by the provided scripts (00–07)
requests>=2.31,<3
tqdm>=4.66,<5
numpy>=1.26,<3
pandas>=2.2,<3
matplotlib>=3.8,<4
eikon

# Needed on Windows for Python's zoneinfo time zones
tzdata; platform_system == "Windows"
