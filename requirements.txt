# Python dependencies for the Polymarket Earnings Study pipeline
# Install with:
#   python -m pip install -r requirements.txt
#
# Recommended Python: 3.10-3.12. The Refinitiv Eikon Python package may lag
# newest Python releases, so avoid very new Python versions unless verified.

requests>=2.31

tqdm>=4.66

pandas>=2.0

numpy>=1.24

# Required only for Refinitiv/Eikon-dependent steps.
# Users also need Refinitiv Workspace or Eikon Desktop installed, running,
# logged in, and a valid app key.
eikon>=1.1.18
